# ask.anuran console assistant — Groq (free, OpenAI-compatible)
# Create a free key at https://console.groq.com/keys (no card required).
# Local dev: copy this file to .env.local and paste your key.
# Production: set GROQ_API_KEY in the Vercel project's Environment Variables.
GROQ_API_KEY=

# Optional — override the default model (openai/gpt-oss-120b)
# GROQ_MODEL=openai/gpt-oss-120b
